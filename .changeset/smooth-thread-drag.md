---
"bb-plugin-ribbon-sidebar": patch
---

Match bb's thread reordering interaction with a cursor-following title chip, row-sized drop previews, touch and keyboard controls, and click suppression after dragging. Dropping on a group title places the thread first, with the preview beneath the title.
