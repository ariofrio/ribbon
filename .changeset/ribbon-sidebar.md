---
"bb-plugin-ribbon-sidebar": minor
---

Add Ribbon sidebar as the suite's exclusive thread-list provider, with
independent scope and grouping, provider discovery, generic placement RPC and
CLI, durable ordering, one-way Thread stages migration, and parity with the
compatible Thread stages renderer's scope synchronization, collapsed previews
and activity, search behavior, and Section icons.
